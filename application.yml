spring:
  profiles:
    active: 
      # - dev

---

spring:
  profiles: dev
  application:
    name: springcloud-server-config-dev-dynamic

---

spring:
  profiles: test
  application:
    name: springcloud-server-config-test
