spring:
  profiles:
    active: 
      # - dev

---

server:
  port: 8201
spring:
  profiles: dev
  application:
    name: springcloud-server-config-client-dev

---

server:
  port: 8202
spring:
  profiles: test
  application:
    name: springcloud-server-config-client-test
